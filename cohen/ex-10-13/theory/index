§   COHEN-EX-10-13

+   N : ℕ

+   a : [0,N) → 𝔹

+   p : [0,N] → 𝔹

    p = ( n : n ∈ [0,N] : ∀( i,j : 0 ≤ i ≤ j < n : a.i ⇒ a.j ) )

[0] p.0 ≡ true

[1] p.(n+1) ≡ p.n ∧ (q.(n+1) ∨ a.n)

    ⚠   n ∈ [0,N)

+   q : [0,N] → 𝔹

    q = ( n : n ∈ [0,N] : ∀( i : 0 ≤ i ≤ n-1 : ¬a.i ) )

[2] q.0 ≡ true

[3] q.(n+1) ≡ q.n ∧ ¬a.n

    ⚠   n ∈ [0,N)

+   inv : [0,N] → 𝔹 → 𝔹 → 𝔹
    
    inv = ( n,z,y : : z ≡ p.n ∧ y ≡ q.n )

[4] { N ≥ 0 } n,z,y:= true,true,0 { inv(n,z,y) }

[5] { inv.(n,z,y) ∧ n ≠ N }

      z,y:= z ∧ (y ∨ a.n), y ∧ ¬a.n

    { inv.(n+1,z,y) }

■   COHEN-EX-10-13